import { IconButton, styled } from '@mui/material'
import { grey } from '@mui/material/colors'

export const IconButtonPopup = styled(IconButton)({
  width: 48,
  height: 48,
  border: `solid 1px ${grey[200]}`
})
